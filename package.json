{
  "name": "note-taker",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"
  },
  "description": "Allows the user to input, read, and delete notes.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "nodemon server.js"
  },
  "author": "Charles Fisher",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
